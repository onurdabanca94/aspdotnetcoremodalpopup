@model IEnumerable<string>
@foreach(var notification in Model)
{
    <div class="alert alert-primary" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Çıkış">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
}

